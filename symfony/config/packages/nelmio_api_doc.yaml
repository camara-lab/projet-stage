nelmio_api_doc:
    documentation:
        info:
            title: Bus Booking API
            description: API REST de réservation de bus (Symfony 7.1)
            version: 1.0.0
        components:
            securitySchemes:
                Bearer:
                    type: http
                    scheme: bearer
                    bearerFormat: JWT
    areas:
        path_patterns:
            - ^/api(?!/doc)
